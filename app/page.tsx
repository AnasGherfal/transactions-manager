// app/page.tsx
import { redirect } from "next/navigation"

export default function RootPage() {
  // In the future, check auth here. For now, send straight to dashboard.
//   redirect("/companies") 
}